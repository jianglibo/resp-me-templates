% layout 'default';

%= include 'layouts/_navigate'

<h2>帮助</h2>
<div class="no-border">
<p>
初始用户是 admin，具有所有权限。可以增删用户，设置用户的流量限额等。
</p>
<p>
每个用户都可以登录此系统，下载客户端和配置文件，然后使用客户端连接到此系统。
</p>

<p>
如果用户无法试用服务，请先确定用户的quota（流量限额）有余额。
</p>
</div>